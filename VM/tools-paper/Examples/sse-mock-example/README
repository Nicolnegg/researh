# SSE Mock Example

This example is meant to test that **c2bc** generates a BINSEC SSE script
compatible with the *new* BINSEC (`-sse -sse-script`).

Steps:
1) Build and run:
   ```console
   $ c2bc -i example.c --run-abduce --forward-to-runner "with-inequalities" --forward-to-runner "max-depth=2"
   c2bc -i example.c --run-abduce --auto-control-variables

   ./example.dir/example.abduce-run.bash --max-depth 2

   ```
   Or run the generated script directly:
   ```console
   $ ./example.dir/example.abduce-run.bash --with-inequalities --max-depth 2
   ```

2) Inspect the generated SSE script:
   - `example.dir/example.binsec.sse`
   - `example.dir/example.robust.sse`

Notes:
- Make sure the **new** `binsec` is in your `PATH`.
- The `.binsec.sse` file is the concatenation of the `.binsec.config` and `.binsec.memory` files.
